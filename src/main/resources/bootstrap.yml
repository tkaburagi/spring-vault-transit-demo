spring.cloud.vault:
#  authentication: approle
#  app-role:
#      role-id:
#      secret-id:
  token: ${VAULT_TOKEN}
  scheme: https
  host: ${VAULT_HOST}
  port: ${VAULT_PORT}
  database:
    enabled: true
    role: role-demoapp
    backend: database